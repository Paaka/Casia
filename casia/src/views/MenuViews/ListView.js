import React from 'react';

import MainTemplate from '../../components/templates/MainTemplate/MainTemplate';
const ListView = props => {
  return (
    <MainTemplate>
      <h1>Hello ListView</h1>
    </MainTemplate>
  );
};

export default ListView;
